# macOS
.DS_Store

# Xcode user-specific state
xcuserdata/

# Build artifacts
DerivedData/
build/
.build/
*.xcuserstate

# Large test fixtures (real camera DNG files — keep out of VCS)
pikicamTests/Fixtures/*.DNG

# Agent runtime session state
.pi-glla/
